---
import projects from "../../moks/projects.json";
---

<div>
    <h2>{projects[0].title}</h2>
    <p>
        {projects[0].description}
    </p>
    <!-- Language -->
    <div>
        {projects[0].language.map((l) => <span class="">{l.title}</span>)}
    </div>
    <!-- Link -->
     <div>
        <a href=`${projects[0].link}`>Live demo</a>
        <a href=`${projects[0].github}`>GitHub</a>
     </div>
</div>
